= render 'layouts/messages'

- content_for :title do
  Listing profiles

%h1 Listing profiles

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @profiles.each do |profile|
      %tr
        %td= link_to 'Show', show_profile_path(profile.username)
%br

= link_to 'New Profile', new_profile_path
